# Base URL of the Template Creation Service backend (used when not using Vite proxy).
# Example: http://localhost:8080
# For local dev with proxy: the dev server proxies /api to this URL.
VITE_API_BASE_URL=http://localhost:8080
