package com.eca.template.application.usecase;

/**
 * Use case: export template schema as Excel (load schema + generate workbook + stream).
 * Delegates Excel generation to infrastructure; application layer only orchestrates.
 */
public interface ExportExcel {
    // To be implemented in later slices.
}
