package com.yourcompany.template.application.usecase;

/**
 * Use case: update schema (validate + canonicalize + hash + persist).
 * Orchestrates validation, hashing, and persistence.
 */
public interface UpdateSchema {
    // To be implemented in later slices.
}
